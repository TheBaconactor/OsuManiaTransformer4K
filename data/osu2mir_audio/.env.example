# osu! API v2 OAuth credentials
# Recommended: copy this file to `config/osu_api.env` (repo root, gitignored).
# Fallback (supported): copy to `data/osu2mir_audio/.env` (also gitignored).
OSU_CLIENT_ID=your_client_id_here
OSU_CLIENT_SECRET=your_client_secret_here
