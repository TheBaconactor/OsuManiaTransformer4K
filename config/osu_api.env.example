# osu! API v2 OAuth credentials (client credentials grant)
#
# Create a new OAuth application on osu! and copy its client id/secret here.
# Callback URL is not used for client-credentials; "http://localhost" is fine.
#
# IMPORTANT: Copy this file to `config/osu_api.env` and fill real values.
# That file is gitignored.
OSU_CLIENT_ID=your_client_id_here
OSU_CLIENT_SECRET=your_client_secret_here

